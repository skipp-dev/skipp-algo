# C7/T8 — minimal runtime deps for the read-only dashboard surface.
# Streamlit + numpy is enough; the dashboard reads pre-computed JSON
# artefacts and does not call into any provider client at runtime.
streamlit>=1.30,<2.0
numpy>=1.26
pandas>=2.0
plotly>=5.18
