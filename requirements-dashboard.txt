# C7/T8 — minimal runtime deps for the read-only dashboard surface.
# The dashboard reads pre-computed JSON artefacts and does not call
# into any provider client at runtime, so we ship Streamlit + numpy +
# pandas + plotly only (no httpx, pandas-ta, databento, etc.).
#
# Supply-chain policy (2026-06-14): these are exact runtime pins for the
# dashboard image. They are intentionally curated for the slim dashboard
# surface and do not imply lockstep equality with every corresponding pin in
# the repo-wide requirements.txt.
streamlit==1.58.0
numpy==2.4.6
pandas==3.0.3
plotly==6.8.0
