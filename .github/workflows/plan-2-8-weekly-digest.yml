name: plan-2.8 weekly trend digest

# Weekly Plan 2.8 trend digest. Pulls the long-running history JSONL
# from the most-recent rolling-bench run, renders the digest with
# scripts/plan_2_8_trend_digest.py, streams it into the run summary
# and uploads the markdown as an artifact for offline review.

on:
  schedule:
    - cron: "0 12 * * 1"   # Mondays, 12:00 UTC
  workflow_dispatch:
    inputs:
      lookback_days:
        description: "Days to look back for the comparison endpoint."
        required: false
        default: "7"
      alert_threshold_pp:
        description: "Per-slice drift threshold for alerts (pp)."
        required: false
        default: "0.05"
      min_events:
        description: "Minimum events per slice for a comparable verdict."
        required: false
        default: "30"

permissions:
  contents: read
  actions: read
  issues: write

jobs:
  weekly-digest:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Download latest rolling-bench artifact
        id: download
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow: smc-measurement-benchmark-rolling.yml
          name_is_regexp: true
          name: '^smc-measurement-benchmark-rolling-.*$'
          workflow_conclusion: ""
          if_no_artifact_found: warn
          path: artifacts/latest_bench

      - name: Render weekly trend digest
        id: digest
        shell: bash
        run: |
          set +e
          mkdir -p artifacts/plan_2_8_digest
          history="$(find artifacts/latest_bench -name plan_2_8_history.jsonl -print -quit)"
          if [ -z "${history}" ]; then
            echo "No plan_2_8_history.jsonl found in latest bench artifact." \
              | tee -a "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          python scripts/plan_2_8_trend_digest.py \
            --history            "${history}" \
            --lookback-days      "${{ inputs.lookback_days || '7' }}" \
            --alert-threshold-pp "${{ inputs.alert_threshold_pp || '0.05' }}" \
            --min-events         "${{ inputs.min_events || '30' }}" \
            --output             artifacts/plan_2_8_digest/weekly_digest.md \
            > /dev/null
          # Also render the issue-body + alerts summary for downstream steps.
          python scripts/plan_2_8_trend_digest.py \
            --history            "${history}" \
            --lookback-days      "${{ inputs.lookback_days || '7' }}" \
            --alert-threshold-pp "${{ inputs.alert_threshold_pp || '0.05' }}" \
            --min-events         "${{ inputs.min_events || '30' }}" \
            --format             issue \
            --output             artifacts/plan_2_8_digest/issue_body.md \
            --alerts-file        artifacts/plan_2_8_digest/alerts.json \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/weekly_digest.md ]; then
            cat artifacts/plan_2_8_digest/weekly_digest.md >> "$GITHUB_STEP_SUMMARY"
          fi
          # Also render a JSON digest so downstream steps can apply
          # the operator snooze config without re-reading the history.
          python scripts/plan_2_8_trend_digest.py \
            --history            "${history}" \
            --lookback-days      "${{ inputs.lookback_days || '7' }}" \
            --alert-threshold-pp "${{ inputs.alert_threshold_pp || '0.05' }}" \
            --min-events         "${{ inputs.min_events || '30' }}" \
            --format             json \
            --output             artifacts/plan_2_8_digest/digest.json \
            > /dev/null
          true

      - name: Lint snooze config
        if: always()
        shell: bash
        run: |
          set +e
          mkdir -p artifacts/plan_2_8_digest
          if [ ! -s configs/plan_2_8_snoozes.json ]; then
            echo "No snooze config; skipping lint."
            exit 0
          fi
          python scripts/plan_2_8_snooze_lint.py \
            --config configs/plan_2_8_snoozes.json \
            --warn-only \
            --output artifacts/plan_2_8_digest/snooze_lint.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/snooze_lint.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/snooze_lint.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Apply alert snooze config
        id: snooze
        if: always()
        shell: bash
        run: |
          set +e
          config="configs/plan_2_8_snoozes.json"
          digest="artifacts/plan_2_8_digest/digest.json"
          if [ ! -s "${digest}" ]; then
            echo "No digest.json to snooze; skipping."
            echo "applied=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          # Pass-through when config missing or has no entries.
          if [ ! -s "${config}" ] || ! python -c "import json,sys;c=json.load(open('${config}'));sys.exit(0 if c.get('snoozes') else 1)" 2>/dev/null; then
            echo "No active snooze entries; pass-through."
            echo "applied=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          python scripts/plan_2_8_alert_snooze.py \
            --digest "${digest}" \
            --snooze "${config}" \
            --output artifacts/plan_2_8_digest/digest.snoozed.json \
            > /dev/null
          # Re-render issue body and alerts file from the filtered digest.
          python -c "
          import json
          from pathlib import Path
          import importlib.util
          spec = importlib.util.spec_from_file_location('td', 'scripts/plan_2_8_trend_digest.py')
          m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m)
          d = json.load(open('artifacts/plan_2_8_digest/digest.snoozed.json'))
          Path('artifacts/plan_2_8_digest/issue_body.md').write_text(m.render_issue_body(d), encoding='utf-8')
          Path('artifacts/plan_2_8_digest/alerts.json').write_text(
            json.dumps({'has_alerts': m.has_alerts(d),
                        'count': len(d.get('alerts') or [])}, indent=2) + '\n',
            encoding='utf-8',
          )
          "
          echo "applied=true" >> "$GITHUB_OUTPUT"
          true

      - name: Resolve final has_alerts
        id: resolve_alerts
        if: always()
        shell: bash
        run: |
          set +e
          alerts="artifacts/plan_2_8_digest/alerts.json"
          has=$(python -c "import json;print(json.load(open('${alerts}'))['has_alerts'])" 2>/dev/null || echo False)
          echo "has_alerts=${has}" >> "$GITHUB_OUTPUT"
          echo "Final has_alerts=${has}"
          true

      - name: Upload weekly digest
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-digest
          path: artifacts/plan_2_8_digest/weekly_digest.md
          if-no-files-found: warn
          retention-days: 90

      - name: Append alerts to history log
        if: always()
        env:
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          set +e
          snoozed="artifacts/plan_2_8_digest/digest.snoozed.json"
          digest="artifacts/plan_2_8_digest/digest.json"
          src="${snoozed}"
          [ -s "${src}" ] || src="${digest}"
          if [ ! -s "${src}" ]; then
            echo "No digest payload available; skipping alert-history append."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_alert_history
          # Re-use the rolling-bench history file's captured_at as the
          # alert key — stable across replays of the same digest run.
          history="$(find artifacts/latest_bench -name plan_2_8_history.jsonl -print -quit)"
          captured="$(python -c "import json,sys; d=json.load(open('${src}')); print(d.get('coverage',{}).get('latest_captured_at') or '')")"
          if [ -z "${captured}" ]; then
            captured="$(date -u +%Y-%m-%dT%H:%M:%SZ)"
          fi
          python scripts/plan_2_8_alert_history.py \
            --alerts     "${src}" \
            --log        artifacts/plan_2_8_alert_history/alert_history.jsonl \
            --captured-at "${captured}" \
            --run-url    "${RUN_URL}" \
            || true
          true

      - name: Upload alert history log
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-alert-history
          path: artifacts/plan_2_8_alert_history/alert_history.jsonl
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 alert-history summary (90-day window)
        if: always()
        shell: bash
        run: |
          set +e
          log="artifacts/plan_2_8_alert_history/alert_history.jsonl"
          if [ ! -s "${log}" ]; then
            echo "No alert history yet; skipping summary."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_alert_history_summary.py \
            --log           "${log}" \
            --lookback-days 90 \
            --top-n         10 \
            --output        artifacts/plan_2_8_digest/alert_history_summary.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/alert_history_summary.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/alert_history_summary.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Plan 2.8 rollout health
        if: always()
        shell: bash
        run: |
          set +e
          d="artifacts/plan_2_8_digest/digest.json"
          c="artifacts/plan_2_8_digest/coverage.json"
          s="artifacts/plan_2_8_digest/stability.json"
          args=""
          [ -s "${d}" ] && args="${args} --digest ${d}"
          [ -s "${c}" ] && args="${args} --coverage ${c}"
          [ -s "${s}" ] && args="${args} --stability ${s}"
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_health.py ${args} \
            --format json \
            --output artifacts/plan_2_8_digest/health.json \
            > /dev/null
          python scripts/plan_2_8_health.py ${args} \
            --format md \
            --output artifacts/plan_2_8_digest/health.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/health.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/health.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Assemble weekly runcard
        if: always()
        env:
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          set +e
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_weekly_runcard.py \
            --artifact-dir artifacts/plan_2_8_digest \
            --output       artifacts/plan_2_8_digest/runcard.md \
            --run-url      "${RUN_URL}" \
            > /dev/null
          true

      - name: Upload weekly runcard
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-runcard
          path: artifacts/plan_2_8_digest/runcard.md
          if-no-files-found: ignore
          retention-days: 180

      - name: Plan 2.8 runcard section index
        if: always()
        shell: bash
        run: |
          set +e
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_runcard_index.py \
            --artifact-dir artifacts/plan_2_8_digest \
            --format       json \
            --output       artifacts/plan_2_8_digest/runcard_index.json \
            > /dev/null
          python scripts/plan_2_8_runcard_index.py \
            --artifact-dir artifacts/plan_2_8_digest \
            --format       md \
            --output       artifacts/plan_2_8_digest/runcard_index.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/runcard_index.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/runcard_index.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Plan 2.8 CHANGELOG slice (last 14 days)
        if: always()
        shell: bash
        run: |
          set +e
          if [ ! -s CHANGELOG.md ]; then
            echo "No CHANGELOG.md; skipping slice."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_changelog_digest.py \
            --changelog      CHANGELOG.md \
            --lookback-days  14 \
            --limit          10 \
            --output         artifacts/plan_2_8_digest/changelog_recent.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/changelog_recent.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/changelog_recent.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Plan 2.8 alert-history heatmap (90-day window)
        if: always()
        shell: bash
        run: |
          set +e
          log="artifacts/plan_2_8_alert_history/alert_history.jsonl"
          if [ ! -s "${log}" ]; then
            echo "No alert history yet; skipping heatmap."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_alert_history_heatmap.py \
            --log            "${log}" \
            --lookback-days  90 \
            --output         artifacts/plan_2_8_digest/alert_history_heatmap.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/alert_history_heatmap.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/alert_history_heatmap.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Plan 2.8 runbook TOC sidebar
        if: always()
        shell: bash
        run: |
          set +e
          doc="docs/plan_2_8_rollout_runbook.md"
          if [ ! -s "${doc}" ]; then
            echo "No runbook doc; skipping TOC."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_runbook_toc.py \
            --doc    "${doc}" \
            --output artifacts/plan_2_8_digest/runbook_toc.md \
            > /dev/null
          python scripts/plan_2_8_runbook_toc.py \
            --doc    "${doc}" \
            --format json \
            --output artifacts/plan_2_8_digest/runbook_toc.json \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/runbook_toc.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/runbook_toc.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Plan 2.8 status snapshot
        if: always()
        shell: bash
        run: |
          set +e
          mkdir -p artifacts/plan_2_8_digest
          args=""
          for pair in \
            "--health:artifacts/plan_2_8_digest/health.json" \
            "--runcard-index:artifacts/plan_2_8_digest/runcard_index.json" \
            "--coverage:artifacts/plan_2_8_digest/coverage.json" \
            "--digest:artifacts/plan_2_8_digest/digest.json" \
          ; do
            flag="${pair%%:*}"
            path="${pair##*:}"
            [ -s "${path}" ] && args="${args} ${flag} ${path}"
          done
          python scripts/plan_2_8_status_snapshot.py ${args} \
            --format json \
            --output artifacts/plan_2_8_digest/status_snapshot.json \
            > /dev/null
          python scripts/plan_2_8_status_snapshot.py ${args} \
            --format md \
            --output artifacts/plan_2_8_digest/status_snapshot.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/status_snapshot.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/status_snapshot.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 status snapshot
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-status-snapshot
          path: |
            artifacts/plan_2_8_digest/status_snapshot.json
            artifacts/plan_2_8_digest/status_snapshot.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Download Plan 2.8 digest archive
        if: always()
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          name: plan-2-8-digest-archive
          name_is_regexp: true
          path: artifacts/_download/plan_2_8_digest_archive
          workflow: plan-2-8-weekly-digest.yml
          workflow_conclusion: ""
          if_no_artifact_found: warn

      - name: Plan 2.8 digest archive + weekly compare
        if: always()
        shell: bash
        run: |
          set +e
          digest="artifacts/plan_2_8_digest/digest.json"
          if [ ! -s "${digest}" ]; then
            echo "No digest.json; skipping archive+compare."
            exit 0
          fi
          # Prime archive dir with any previously-uploaded snapshots.
          mkdir -p artifacts/plan_2_8_digest_archive
          if compgen -G "artifacts/_download/plan_2_8_digest_archive/**/*.json" > /dev/null; then
            cp -n artifacts/_download/plan_2_8_digest_archive/**/*.json \
              artifacts/plan_2_8_digest_archive/ 2>/dev/null
          fi
          python scripts/plan_2_8_digest_archive.py \
            --digest      "${digest}" \
            --archive-dir artifacts/plan_2_8_digest_archive \
            --keep        26 \
            --emit-latest-two \
            > artifacts/plan_2_8_digest/archive_report.txt
          # Last two lines of the report are the archive paths; diff if >=2.
          paths=$(tail -n 2 artifacts/plan_2_8_digest/archive_report.txt)
          count=$(echo "${paths}" | wc -l | tr -d ' ')
          if [ "${count}" -ge 2 ]; then
            baseline=$(echo "${paths}" | head -n 1)
            current=$( echo "${paths}" | tail -n 1)
            if [ "${baseline}" != "${current}" ]; then
              python scripts/plan_2_8_digest_compare.py \
                --baseline "${baseline}" \
                --current  "${current}" \
                --output   artifacts/plan_2_8_digest/weekly_compare.md \
                > /dev/null
              python scripts/plan_2_8_digest_compare.py \
                --baseline "${baseline}" \
                --current  "${current}" \
                --format   json \
                --output   artifacts/plan_2_8_digest/weekly_compare.json \
                > /dev/null
              if [ -s artifacts/plan_2_8_digest/weekly_compare.md ] \
                  && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
                cat artifacts/plan_2_8_digest/weekly_compare.md \
                  >> "${GITHUB_STEP_SUMMARY}"
              fi
            else
              echo "Only one dated archive; skipping compare."
            fi
          else
            echo "Fewer than two dated archives yet; skipping compare."
          fi
          true

      - name: Upload Plan 2.8 digest archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-archive
          path: artifacts/plan_2_8_digest_archive
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 compact status runcard
        if: always()
        env:
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          set +e
          mkdir -p artifacts/plan_2_8_digest
          args=""
          for pair in \
            "--status-snapshot:artifacts/plan_2_8_digest/status_snapshot.json" \
            "--runcard-index:artifacts/plan_2_8_digest/runcard_index.json" \
            "--health:artifacts/plan_2_8_digest/health.json" \
          ; do
            flag="${pair%%:*}"
            path="${pair##*:}"
            [ -s "${path}" ] && args="${args} ${flag} ${path}"
          done
          python scripts/plan_2_8_runcard_from_status.py ${args} \
            --output  artifacts/plan_2_8_digest/status_runcard.md \
            --run-url "${RUN_URL}" \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/status_runcard.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/status_runcard.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 status runcard
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-status-runcard
          path: artifacts/plan_2_8_digest/status_runcard.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 history CSV export (last 365 days)
        if: always()
        shell: bash
        run: |
          set +e
          history="$(find artifacts/latest_bench -name plan_2_8_history.jsonl -print -quit)"
          if [ -z "${history}" ] || [ ! -s "${history}" ]; then
            echo "No history found; skipping CSV export."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_history_export.py \
            --history       "${history}" \
            --lookback-days 365 \
            --output        artifacts/plan_2_8_digest/history.csv \
            --quiet
          true

      - name: Upload Plan 2.8 history CSV
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-history-csv
          path: artifacts/plan_2_8_digest/history.csv
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 runbook link check
        if: always()
        shell: bash
        run: |
          set +e
          doc="docs/plan_2_8_rollout_runbook.md"
          if [ ! -s "${doc}" ]; then
            echo "No runbook doc; skipping link check."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_runbook_link_check.py \
            --doc    "${doc}" \
            --output artifacts/plan_2_8_digest/runbook_link_check.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/runbook_link_check.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/runbook_link_check.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Plan 2.8 snooze expiry report
        if: always()
        shell: bash
        run: |
          set +e
          cfg="configs/plan_2_8_snoozes.json"
          if [ ! -s "${cfg}" ]; then
            echo "No snooze config; skipping expiry report."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_snooze_expiry_report.py \
            --config      "${cfg}" \
            --within-days 14 \
            --output      artifacts/plan_2_8_digest/snooze_expiry.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/snooze_expiry.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/snooze_expiry.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 snooze expiry report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-snooze-expiry
          path: artifacts/plan_2_8_digest/snooze_expiry.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 script manifest
        if: always()
        shell: bash
        run: |
          set +e
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_manifest.py \
            --format json \
            --output artifacts/plan_2_8_digest/manifest.json \
            > /dev/null
          true

      - name: Upload Plan 2.8 script manifest
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-manifest
          path: artifacts/plan_2_8_digest/manifest.json
          if-no-files-found: ignore
          retention-days: 365

      - name: Download prior Plan 2.8 manifest
        if: always()
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow:       plan-2-8-weekly-digest.yml
          workflow_conclusion: ''
          name:           plan-2-8-manifest
          name_is_regexp: true
          path:           artifacts/plan_2_8_prior_manifest
          if_no_artifact_found: warn

      - name: Plan 2.8 manifest diff
        if: always()
        shell: bash
        run: |
          set +e
          prior="$(find artifacts/plan_2_8_prior_manifest -name manifest.json -print -quit)"
          current="artifacts/plan_2_8_digest/manifest.json"
          if [ -z "${prior}" ] || [ ! -s "${prior}" ] \
              || [ ! -s "${current}" ]; then
            echo "No prior manifest; skipping diff."
            exit 0
          fi
          python scripts/plan_2_8_manifest_diff.py \
            --baseline "${prior}" \
            --current  "${current}" \
            --format   md \
            --output   artifacts/plan_2_8_digest/manifest_diff.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/manifest_diff.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/manifest_diff.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 manifest diff
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-manifest-diff
          path: artifacts/plan_2_8_digest/manifest_diff.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest schema check
        if: always()
        shell: bash
        run: |
          set +e
          digest="$(find artifacts/plan_2_8_digest -name digest.json -print -quit)"
          if [ -z "${digest}" ] || [ ! -s "${digest}" ]; then
            echo "No digest.json; skipping schema check."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_digest_schema.py \
            --digest "${digest}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_schema.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/digest_schema.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/digest_schema.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 digest schema report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-schema-report
          path: artifacts/plan_2_8_digest/digest_schema.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 shields.io status badge
        if: always()
        shell: bash
        run: |
          set +e
          snap="artifacts/plan_2_8_digest/status_snapshot.json"
          health="artifacts/plan_2_8_digest/rollout_health.json"
          if [ -s "${snap}" ]; then
            input="${snap}"
          elif [ -s "${health}" ]; then
            input="${health}"
          else
            echo "No snapshot or health JSON; skipping badge."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_runcard_badge.py \
            --input  "${input}" \
            --output artifacts/plan_2_8_digest/status_badge.json \
            > /dev/null
          true

      - name: Upload Plan 2.8 status badge
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-status-badge
          path: artifacts/plan_2_8_digest/status_badge.json
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 README badge markdown
        if: always()
        shell: bash
        run: |
          set +e
          badge="artifacts/plan_2_8_digest/status_badge.json"
          if [ ! -s "${badge}" ]; then
            echo "No status badge JSON; skipping README badge."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          endpoint="https://raw.githubusercontent.com/${GITHUB_REPOSITORY}/main/artifacts/plan_2_8_digest/status_badge.json"
          python scripts/plan_2_8_badge_markdown.py \
            --endpoint-url "${endpoint}" \
            --output       artifacts/plan_2_8_digest/badge.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 README badge markdown
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-badge-markdown
          path: artifacts/plan_2_8_digest/badge.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 alert trend
        if: always()
        shell: bash
        run: |
          set +e
          archive="artifacts/plan_2_8_digest_archive"
          if [ ! -d "${archive}" ]; then
            echo "No digest archive; skipping alert trend."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_alert_trend.py \
            --archive-dir "${archive}" \
            --format      md \
            --output      artifacts/plan_2_8_digest/alert_trend.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/alert_trend.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/alert_trend.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 alert trend
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-alert-trend
          path: artifacts/plan_2_8_digest/alert_trend.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 alert trend gate
        if: always()
        shell: bash
        run: |
          set +e
          trend_md="artifacts/plan_2_8_digest/alert_trend.md"
          archive="artifacts/plan_2_8_digest_archive"
          if [ ! -d "${archive}" ]; then
            echo "No archive; skipping alert trend gate."
            exit 0
          fi
          python scripts/plan_2_8_alert_trend.py \
            --archive-dir "${archive}" \
            --format      json \
            --output      artifacts/plan_2_8_digest/alert_trend.json \
            > /dev/null
          if [ ! -s artifacts/plan_2_8_digest/alert_trend.json ]; then
            echo "No trend JSON; skipping alert trend gate."
            exit 0
          fi
          python scripts/plan_2_8_alert_trend_gate.py \
            --trend       artifacts/plan_2_8_digest/alert_trend.json \
            --max-rising  5 \
            --max-new     10 \
            --format      md \
            --output      artifacts/plan_2_8_digest/alert_trend_gate.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/alert_trend_gate.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/alert_trend_gate.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 alert trend gate
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-alert-trend-gate
          path: artifacts/plan_2_8_digest/alert_trend_gate.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest vs coverage projection
        if: always()
        shell: bash
        run: |
          set +e
          digest="$(find artifacts/plan_2_8_digest -name digest.json -print -quit)"
          coverage="$(find artifacts/plan_2_8_digest -name 'coverage*.json' -print -quit)"
          if [ -z "${digest}" ] || [ -z "${coverage}" ] \
              || [ ! -s "${digest}" ] || [ ! -s "${coverage}" ]; then
            echo "Digest or coverage missing; skipping projection."
            exit 0
          fi
          python scripts/plan_2_8_digest_to_coverage.py \
            --digest   "${digest}" \
            --coverage "${coverage}" \
            --format   md \
            --output   artifacts/plan_2_8_digest/digest_vs_coverage.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/digest_vs_coverage.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/digest_vs_coverage.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 digest vs coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-vs-coverage
          path: artifacts/plan_2_8_digest/digest_vs_coverage.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 runbook section check
        if: always()
        shell: bash
        run: |
          set +e
          doc="docs/plan_2_8_rollout_runbook.md"
          if [ ! -s "${doc}" ]; then
            echo "No runbook; skipping section check."
            exit 0
          fi
          python scripts/plan_2_8_runbook_sections.py \
            --doc    "${doc}" \
            --format md \
            --output artifacts/plan_2_8_digest/runbook_sections.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/runbook_sections.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/runbook_sections.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 runbook section report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-runbook-sections
          path: artifacts/plan_2_8_digest/runbook_sections.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Download prior Plan 2.8 status ledger
        if: always()
        continue-on-error: true
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow:       plan-2-8-weekly-digest.yml
          workflow_conclusion: ''
          name:           plan-2-8-status-ledger
          name_is_regexp: true
          path:           artifacts/plan_2_8_prior_ledger
          if_no_artifact_found: warn

      - name: Plan 2.8 status ledger append
        if: always()
        shell: bash
        env:
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set +e
          snap="artifacts/plan_2_8_digest/status_snapshot.json"
          health="artifacts/plan_2_8_digest/rollout_health.json"
          if [ -s "${snap}" ]; then
            input="${snap}"
          elif [ -s "${health}" ]; then
            input="${health}"
          else
            echo "No snapshot or health JSON; skipping ledger append."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          prior="$(find artifacts/plan_2_8_prior_ledger -name status_ledger.jsonl -print -quit)"
          if [ -n "${prior}" ] && [ -s "${prior}" ]; then
            cp "${prior}" "${ledger}"
          fi
          python scripts/plan_2_8_status_ledger.py \
            --input   "${input}" \
            --ledger  "${ledger}" \
            --run-url "${RUN_URL}" \
            > /dev/null
          true

      - name: Upload Plan 2.8 status ledger
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-status-ledger
          path: artifacts/plan_2_8_digest/status_ledger.jsonl
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 status ledger summary
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger to summarise; skipping."
            exit 0
          fi
          python scripts/plan_2_8_status_ledger_summarize.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/status_ledger_summary.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/status_ledger_summary.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/status_ledger_summary.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 status ledger summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-status-ledger-summary
          path: artifacts/plan_2_8_digest/status_ledger_summary.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 status ledger flip alert
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping flip alert."
            exit 0
          fi
          python scripts/plan_2_8_status_flip_alert.py \
            --ledger "${ledger}" \
            --weeks  12 \
            --format md \
            --output artifacts/plan_2_8_digest/status_flip_alert.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/status_flip_alert.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/status_flip_alert.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 status flip alert
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-status-flip-alert
          path: artifacts/plan_2_8_digest/status_flip_alert.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger CSV export
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping CSV export."
            exit 0
          fi
          python scripts/plan_2_8_ledger_csv_export.py \
            --ledger "${ledger}" \
            --output artifacts/plan_2_8_digest/status_ledger.csv
          true

      - name: Upload Plan 2.8 ledger CSV
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-csv
          path: artifacts/plan_2_8_digest/status_ledger.csv
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 status ledger prune
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger to prune; skipping."
            exit 0
          fi
          python scripts/plan_2_8_status_ledger_prune.py \
            --ledger "${ledger}" --keep 104
          true

      - name: Plan 2.8 run stamp
        if: always()
        shell: bash
        env:
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          set +e
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_run_stamp.py \
            --output  artifacts/plan_2_8_digest/run_stamp.json \
            --run-url "${RUN_URL}" \
            --quiet
          true

      - name: Upload Plan 2.8 run stamp
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-run-stamp
          path: artifacts/plan_2_8_digest/run_stamp.json
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly artifact index
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          if [ ! -d "${dir}" ]; then
            echo "No artifact dir; skipping index."
            exit 0
          fi
          python scripts/plan_2_8_weekly_index.py \
            --artifact-dir "${dir}" \
            --format       md \
            --output       "${dir}/index.md" \
            > /dev/null
          python scripts/plan_2_8_weekly_index.py \
            --artifact-dir "${dir}" \
            --format       json \
            --output       "${dir}/index.json" \
            > /dev/null
          if [ -s "${dir}/index.md" ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat "${dir}/index.md" >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 weekly artifact index
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-index
          path: |
            artifacts/plan_2_8_digest/index.md
            artifacts/plan_2_8_digest/index.json
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 artifact checksums
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          if [ ! -d "${dir}" ]; then
            echo "No artifact dir; skipping checksums."
            exit 0
          fi
          python scripts/plan_2_8_artifact_checksum.py \
            --artifact-dir "${dir}" \
            --json-output  "${dir}/checksums.json" \
            --md-output    "${dir}/checksums.md" \
            --skip         checksums.json,checksums.md
          true

      - name: Upload Plan 2.8 artifact checksums
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-artifact-checksums
          path: |
            artifacts/plan_2_8_digest/checksums.json
            artifacts/plan_2_8_digest/checksums.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest archive index
        if: always()
        shell: bash
        run: |
          set +e
          archive="artifacts/plan_2_8_digest_archive"
          if [ ! -d "${archive}" ]; then
            echo "No digest archive dir; skipping index."
            exit 0
          fi
          python scripts/plan_2_8_digest_archive_index.py \
            --archive-dir "${archive}" \
            --format      md \
            --output      "artifacts/plan_2_8_digest/archive_index.md" \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest archive index
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-archive-index
          path: artifacts/plan_2_8_digest/archive_index.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest size budget
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          if [ ! -d "${dir}" ]; then
            echo "No artifact dir; skipping size budget."
            exit 0
          fi
          python scripts/plan_2_8_digest_size_budget.py \
            --artifact-dir "${dir}" \
            --max-bytes    1048576 \
            --format       md \
            --output       "${dir}/size_budget.md" \
            > /dev/null
          if [ -s "${dir}/size_budget.md" ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat "${dir}/size_budget.md" >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 digest size budget
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-size-budget
          path: artifacts/plan_2_8_digest/size_budget.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger downtime
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping downtime."
            exit 0
          fi
          python scripts/plan_2_8_ledger_downtime.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/downtime.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger downtime
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-downtime
          path: artifacts/plan_2_8_digest/downtime.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Download prior Plan 2.8 weekly index
        if: always()
        uses: dawidd6/action-download-artifact@v6
        with:
          name: plan-2-8-weekly-index
          name_is_regexp: true
          path: artifacts/plan_2_8_prior_index
          workflow_conclusion: ''
          if_no_artifact_found: warn

      - name: Plan 2.8 weekly-index diff
        if: always()
        shell: bash
        run: |
          set +e
          current="artifacts/plan_2_8_digest/index.json"
          prior="artifacts/plan_2_8_prior_index/index.json"
          if [ ! -s "${current}" ]; then
            # try nested path from download
            current_alt=$(find artifacts/plan_2_8_digest \
              -maxdepth 3 -name index.json 2>/dev/null | head -1)
            if [ -n "${current_alt}" ]; then
              current="${current_alt}"
            else
              echo "No current index.json; skipping diff."
              exit 0
            fi
          fi
          if [ ! -s "${prior}" ]; then
            prior_alt=$(find artifacts/plan_2_8_prior_index \
              -maxdepth 3 -name index.json 2>/dev/null | head -1)
            if [ -n "${prior_alt}" ]; then
              prior="${prior_alt}"
            fi
          fi
          python scripts/plan_2_8_digest_index_compare.py \
            --prior   "${prior}" \
            --current "${current}" \
            --format  md \
            --output  artifacts/plan_2_8_digest/index_diff.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly-index diff
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-index-diff
          path: artifacts/plan_2_8_digest/index_diff.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger uptime
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping uptime."
            exit 0
          fi
          python scripts/plan_2_8_ledger_uptime_pct.py \
            --ledger "${ledger}" \
            --weeks  4 \
            --format md \
            --output artifacts/plan_2_8_digest/uptime.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger uptime
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-uptime
          path: artifacts/plan_2_8_digest/uptime.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary index
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          if [ ! -d "${dir}" ]; then
            echo "No artifact dir; skipping summary index."
            exit 0
          fi
          python scripts/plan_2_8_weekly_summary_index.py \
            --artifact-dir "${dir}" \
            --output       "${dir}/weekly_summary.md" \
            --quiet
          true

      - name: Upload Plan 2.8 weekly summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary
          path: artifacts/plan_2_8_digest/weekly_summary.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 latest status
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping latest-status."
            exit 0
          fi
          python scripts/plan_2_8_ledger_latest_status.py \
            --ledger "${ledger}" \
            --format json \
            --output artifacts/plan_2_8_digest/latest_status.json \
            > /dev/null
          true

      - name: Upload Plan 2.8 latest status
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-latest-status
          path: artifacts/plan_2_8_digest/latest_status.json
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest metadata
        if: always()
        shell: bash
        run: |
          set +e
          python scripts/plan_2_8_digest_metadata.py \
            --scripts-dir scripts \
            --format json \
            --output artifacts/plan_2_8_digest/metadata.json \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest metadata
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-metadata
          path: artifacts/plan_2_8_digest/metadata.json
          if-no-files-found: ignore
          retention-days: 365

      - name: Download prior Plan 2.8 metadata
        if: always()
        uses: dawidd6/action-download-artifact@v6
        with:
          name: plan-2-8-digest-metadata
          name_is_regexp: true
          workflow: plan-2-8-weekly-digest.yml
          workflow_conclusion: ''
          path: artifacts/plan_2_8_digest_prior_metadata
          if_no_artifact_found: warn

      - name: Plan 2.8 metadata diff
        if: always()
        shell: bash
        run: |
          set +e
          current="artifacts/plan_2_8_digest/metadata.json"
          if [ ! -s "${current}" ]; then
            echo "No current metadata; skipping diff."
            exit 0
          fi
          prior="$(find artifacts/plan_2_8_digest_prior_metadata \
            -name metadata.json -print -quit 2>/dev/null)"
          if [ -z "${prior}" ]; then
            prior="/dev/null"
          fi
          python scripts/plan_2_8_metadata_diff.py \
            --prior   "${prior}" \
            --current "${current}" \
            --format  md \
            --output  artifacts/plan_2_8_digest/metadata_diff.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 metadata diff
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-metadata-diff
          path: artifacts/plan_2_8_digest/metadata_diff.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger trend
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping trend."
            exit 0
          fi
          python scripts/plan_2_8_ledger_trend.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/trend.md \
            > /dev/null
          python scripts/plan_2_8_ledger_trend.py \
            --ledger "${ledger}" \
            --format json \
            --output artifacts/plan_2_8_digest/trend.json \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger trend
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-trend
          path: artifacts/plan_2_8_digest/trend.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly-summary link check
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          if [ ! -s "${summary}" ]; then
            echo "No weekly summary; skipping link check."
            exit 0
          fi
          python scripts/plan_2_8_weekly_summary_linkcheck.py \
            --input  "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/weekly_summary_linkcheck.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly-summary link check
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-linkcheck
          path: artifacts/plan_2_8_digest/weekly_summary_linkcheck.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger flap rate
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping flap rate."
            exit 0
          fi
          python scripts/plan_2_8_ledger_flap_rate.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/flap_rate.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger flap rate
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-flap-rate
          path: artifacts/plan_2_8_digest/flap_rate.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 trend-threshold alert
        if: always()
        shell: bash
        run: |
          set +e
          trend="artifacts/plan_2_8_digest/trend.json"
          if [ ! -s "${trend}" ]; then
            echo "No trend JSON; skipping threshold alert."
            exit 0
          fi
          python scripts/plan_2_8_trend_threshold_alert.py \
            --trend-json "${trend}" \
            --threshold  90 \
            --format     md \
            --output     artifacts/plan_2_8_digest/trend_threshold.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 trend-threshold alert
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-trend-threshold-alert
          path: artifacts/plan_2_8_digest/trend_threshold.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest artifact catalog
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          if [ ! -d "${dir}" ]; then
            echo "No artifact dir; skipping catalog."
            exit 0
          fi
          python scripts/plan_2_8_digest_artifact_catalog.py \
            --artifact-dir "${dir}" \
            --format       md \
            --output       "${dir}/artifact_catalog.md" \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest artifact catalog
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-artifact-catalog
          path: artifacts/plan_2_8_digest/artifact_catalog.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 status today
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping status-today."
            exit 0
          fi
          python scripts/plan_2_8_ledger_status_today.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/status_today.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 status today
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-status-today
          path: artifacts/plan_2_8_digest/status_today.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 recent status changes
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping recent-changes."
            exit 0
          fi
          python scripts/plan_2_8_digest_recent_changes.py \
            --ledger "${ledger}" \
            --limit  10 \
            --format md \
            --output artifacts/plan_2_8_digest/recent_changes.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 recent status changes
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-recent-changes
          path: artifacts/plan_2_8_digest/recent_changes.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly-summary TOC only
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          if [ ! -s "${summary}" ]; then
            echo "No weekly summary; skipping TOC-only."
            exit 0
          fi
          python scripts/plan_2_8_weekly_summary_toc_only.py \
            --input  "${summary}" \
            --output artifacts/plan_2_8_digest/weekly_summary_toc.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/weekly_summary_toc.md ]; then
            cat artifacts/plan_2_8_digest/weekly_summary_toc.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Upload Plan 2.8 weekly-summary TOC
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-toc
          path: artifacts/plan_2_8_digest/weekly_summary_toc.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger streak now
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping streak-now."
            exit 0
          fi
          python scripts/plan_2_8_ledger_streak_now.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/streak_now.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger streak now
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-streak-now
          path: artifacts/plan_2_8_digest/streak_now.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest artifact age
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          if [ ! -d "${dir}" ]; then
            echo "No artifact dir; skipping age report."
            exit 0
          fi
          python scripts/plan_2_8_digest_artifact_age.py \
            --artifact-dir "${dir}" \
            --format       md \
            --output       "${dir}/artifact_age.md" \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest artifact age
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-artifact-age
          path: artifacts/plan_2_8_digest/artifact_age.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger month summary
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping month summary."
            exit 0
          fi
          python scripts/plan_2_8_ledger_month_summary.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/month_summary.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger month summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-month-summary
          path: artifacts/plan_2_8_digest/month_summary.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger worst day
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping worst day."
            exit 0
          fi
          python scripts/plan_2_8_ledger_worst_day.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/worst_day.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger worst day
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-worst-day
          path: artifacts/plan_2_8_digest/worst_day.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Download Plan 2.8 prior artifact catalog
        if: always()
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow:               plan-2-8-weekly-digest.yml
          workflow_conclusion:    ''
          name:                   plan-2-8-digest-artifact-catalog
          name_is_regexp:         true
          path:                   artifacts/plan_2_8_prior
          if_no_artifact_found:   warn

      - name: Plan 2.8 digest catalog diff
        if: always()
        shell: bash
        run: |
          set +e
          prior="artifacts/plan_2_8_prior/artifact_catalog.json"
          curr="artifacts/plan_2_8_digest/artifact_catalog.json"
          if [ ! -s "${curr}" ]; then
            echo "No current catalog; skipping diff."
            exit 0
          fi
          if [ ! -s "${prior}" ]; then
            prior="${curr}"
          fi
          python scripts/plan_2_8_digest_catalog_diff.py \
            --prior   "${prior}" \
            --current "${curr}" \
            --format  md \
            --output  artifacts/plan_2_8_digest/catalog_diff.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest catalog diff
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-catalog-diff
          path: artifacts/plan_2_8_digest/catalog_diff.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary section stats
        if: always()
        shell: bash
        run: |
          set +e
          md="artifacts/plan_2_8_digest/weekly_summary.md"
          if [ ! -s "${md}" ]; then
            echo "No weekly_summary.md; skipping section stats."
            exit 0
          fi
          python scripts/plan_2_8_weekly_summary_section_stats.py \
            --input  "${md}" \
            --format md \
            --output artifacts/plan_2_8_digest/section_stats.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary section stats
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-section-stats
          path: artifacts/plan_2_8_digest/section_stats.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger best day
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping best day."
            exit 0
          fi
          python scripts/plan_2_8_ledger_best_day.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/best_day.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger best day
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-best-day
          path: artifacts/plan_2_8_digest/best_day.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest size trend
        if: always()
        shell: bash
        run: |
          set +e
          prior="artifacts/plan_2_8_prior"
          curr="artifacts/plan_2_8_digest"
          if [ ! -d "${curr}" ]; then
            echo "No current dir; skipping size trend."
            exit 0
          fi
          if [ ! -d "${prior}" ]; then
            prior="${curr}"
          fi
          python scripts/plan_2_8_digest_size_trend.py \
            --prior   "${prior}" \
            --current "${curr}" \
            --format  md \
            --output  artifacts/plan_2_8_digest/size_trend.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest size trend
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-size-trend
          path: artifacts/plan_2_8_digest/size_trend.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary heading order
        if: always()
        shell: bash
        run: |
          set +e
          md="artifacts/plan_2_8_digest/weekly_summary.md"
          if [ ! -s "${md}" ]; then
            echo "No weekly_summary.md; skipping heading-order check."
            exit 0
          fi
          python scripts/plan_2_8_weekly_summary_heading_order.py \
            --input  "${md}" \
            --format md \
            --output artifacts/plan_2_8_digest/heading_order.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary heading order
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-heading-order
          path: artifacts/plan_2_8_digest/heading_order.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger status share
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping status share."
            exit 0
          fi
          python scripts/plan_2_8_ledger_status_share.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/status_share.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger status share
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-status-share
          path: artifacts/plan_2_8_digest/status_share.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest missing artifacts
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          if [ ! -d "${dir}" ]; then
            echo "No artifact dir; skipping missing-artifacts check."
            exit 0
          fi
          python scripts/plan_2_8_digest_missing_artifacts.py \
            --artifact-dir "${dir}" \
            --format       md \
            --output       "${dir}/missing_artifacts.md" \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest missing artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-missing-artifacts
          path: artifacts/plan_2_8_digest/missing_artifacts.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary TOC checksum
        if: always()
        shell: bash
        run: |
          set +e
          md="artifacts/plan_2_8_digest/weekly_summary.md"
          if [ ! -s "${md}" ]; then
            echo "No weekly_summary.md; skipping TOC checksum."
            exit 0
          fi
          python scripts/plan_2_8_weekly_summary_toc_checksum.py \
            --input  "${md}" \
            --format md \
            --output artifacts/plan_2_8_digest/toc_checksum.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary TOC checksum
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-toc-checksum
          path: artifacts/plan_2_8_digest/toc_checksum.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger hour histogram
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping hour histogram."
            exit 0
          fi
          python scripts/plan_2_8_ledger_hour_histogram.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/hour_histogram.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger hour histogram
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-hour-histogram
          path: artifacts/plan_2_8_digest/hour_histogram.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest stale report
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          if [ ! -d "${dir}" ]; then
            echo "No artifact dir; skipping stale report."
            exit 0
          fi
          python scripts/plan_2_8_digest_stale_report.py \
            --artifact-dir "${dir}" \
            --warn-days    7 \
            --stale-days   14 \
            --format       md \
            --output       "${dir}/stale_report.md" \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest stale report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-stale-report
          path: artifacts/plan_2_8_digest/stale_report.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary required sections
        if: always()
        shell: bash
        run: |
          set +e
          md="artifacts/plan_2_8_digest/weekly_summary.md"
          if [ ! -s "${md}" ]; then
            echo "No weekly_summary.md; skipping required-sections check."
            exit 0
          fi
          python scripts/plan_2_8_weekly_summary_required_sections.py \
            --input  "${md}" \
            --format md \
            --output artifacts/plan_2_8_digest/required_sections.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary required sections
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-required-sections
          path: artifacts/plan_2_8_digest/required_sections.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger weekday histogram
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping weekday histogram."
            exit 0
          fi
          python scripts/plan_2_8_ledger_weekday_histogram.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/weekday_histogram.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger weekday histogram
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-weekday-histogram
          path: artifacts/plan_2_8_digest/weekday_histogram.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest summary index
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          if [ ! -d "${dir}" ]; then
            echo "No artifact dir; skipping summary index."
            exit 0
          fi
          python scripts/plan_2_8_digest_summary_index.py \
            --artifact-dir "${dir}" \
            --format       md \
            --output       "${dir}/summary_index.md" \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest summary index
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-summary-index
          path: artifacts/plan_2_8_digest/summary_index.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger gap detector
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/status_ledger.jsonl"
          if [ ! -s "${ledger}" ]; then
            echo "No ledger; skipping gap detector."
            exit 0
          fi
          python scripts/plan_2_8_ledger_gap_detector.py \
            --ledger          "${ledger}" \
            --threshold-hours 24 \
            --format          md \
            --output          artifacts/plan_2_8_digest/gaps.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger gap detector
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-gap-detector
          path: artifacts/plan_2_8_digest/gaps.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger transition matrix
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_ledger/status_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_ledger_transition_matrix.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/transition_matrix.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger transition matrix
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-transition-matrix
          path: artifacts/plan_2_8_digest/transition_matrix.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest hash inventory
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_hash_inventory.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/hash_inventory.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest hash inventory
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-hash-inventory
          path: artifacts/plan_2_8_digest/hash_inventory.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary preview
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_preview.py \
            --summary "${summary}" \
            --max-lines 20 \
            --format md \
            --output artifacts/plan_2_8_digest/summary_preview.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary preview
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-preview
          path: artifacts/plan_2_8_digest/summary_preview.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger latest flip
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_ledger/status_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_ledger_latest_flip.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/latest_flip.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger latest flip
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-latest-flip
          path: artifacts/plan_2_8_digest/latest_flip.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest filetype breakdown
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_filetype_breakdown.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/filetype_breakdown.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest filetype breakdown
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-filetype-breakdown
          path: artifacts/plan_2_8_digest/filetype_breakdown.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary word count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_word_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_word_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary word count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-word-count
          path: artifacts/plan_2_8_digest/summary_word_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger first flip
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_ledger/status_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_ledger_first_flip.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/first_flip.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger first flip
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-first-flip
          path: artifacts/plan_2_8_digest/first_flip.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest largest files
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_largest_files.py \
            --artifact-dir "${dir}" \
            --top-n 10 \
            --format md \
            --output artifacts/plan_2_8_digest/largest_files.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest largest files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-largest-files
          path: artifacts/plan_2_8_digest/largest_files.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary code blocks
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_code_blocks.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_code_blocks.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary code blocks
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-code-blocks
          path: artifacts/plan_2_8_digest/summary_code_blocks.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger status run length
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_ledger/status_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_ledger_status_run_length.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/status_run_length.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger status run length
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-status-run-length
          path: artifacts/plan_2_8_digest/status_run_length.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest smallest files
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_smallest_files.py \
            --artifact-dir "${dir}" \
            --bottom-n 10 \
            --format md \
            --output artifacts/plan_2_8_digest/smallest_files.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest smallest files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-smallest-files
          path: artifacts/plan_2_8_digest/smallest_files.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary link check
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_link_check.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_link_check.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary link check
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-link-check
          path: artifacts/plan_2_8_digest/summary_link_check.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger longest run
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_ledger/status_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_ledger_longest_run.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/longest_run.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger longest run
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-longest-run
          path: artifacts/plan_2_8_digest/longest_run.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest size histogram
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_size_histogram.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/size_histogram.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest size histogram
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-size-histogram
          path: artifacts/plan_2_8_digest/size_histogram.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary list stats
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_list_stats.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_list_stats.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary list stats
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-list-stats
          path: artifacts/plan_2_8_digest/summary_list_stats.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger last-N summary
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_ledger/status_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_ledger_last_n_summary.py \
            --ledger "${ledger}" \
            --last-n 10 \
            --format md \
            --output artifacts/plan_2_8_digest/last_n_summary.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger last-N summary
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-last-n-summary
          path: artifacts/plan_2_8_digest/last_n_summary.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest mean size
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_mean_size.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/mean_size.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest mean size
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-mean-size
          path: artifacts/plan_2_8_digest/mean_size.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary tables count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_tables_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_tables_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary tables count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-tables-count
          path: artifacts/plan_2_8_digest/summary_tables_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger recent green ratio
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_recent_green_ratio.py \
            --ledger "${ledger}" \
            --last-n 10 \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_recent_green_ratio.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger recent green ratio
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-recent-green-ratio
          path: artifacts/plan_2_8_digest/ledger_recent_green_ratio.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest median size
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_median_size.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_median_size.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest median size
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-median-size
          path: artifacts/plan_2_8_digest/digest_median_size.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary emphasis count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_emphasis_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_emphasis_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary emphasis count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-emphasis-count
          path: artifacts/plan_2_8_digest/summary_emphasis_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger status streaks
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_status_streaks.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_status_streaks.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger status streaks
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-status-streaks
          path: artifacts/plan_2_8_digest/ledger_status_streaks.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest oldest/newest
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_oldest_newest.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_oldest_newest.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest oldest/newest
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-oldest-newest
          path: artifacts/plan_2_8_digest/digest_oldest_newest.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary image count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_image_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_image_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary image count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-image-count
          path: artifacts/plan_2_8_digest/summary_image_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger distinct days
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_distinct_days.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_distinct_days.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger distinct days
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-distinct-days
          path: artifacts/plan_2_8_digest/ledger_distinct_days.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest extension coverage
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_extension_coverage.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_extension_coverage.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest extension coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-extension-coverage
          path: artifacts/plan_2_8_digest/digest_extension_coverage.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary paragraph stats
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_paragraph_stats.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_paragraph_stats.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary paragraph stats
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-paragraph-stats
          path: artifacts/plan_2_8_digest/summary_paragraph_stats.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger recent amber ratio
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_amber_ratio.py \
            --ledger "${ledger}" \
            --last-n 10 \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_amber_ratio.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger recent amber ratio
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-recent-amber-ratio
          path: artifacts/plan_2_8_digest/ledger_amber_ratio.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest name length stats
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_name_length_stats.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_name_length_stats.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest name length stats
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-name-length-stats
          path: artifacts/plan_2_8_digest/digest_name_length_stats.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary heading hierarchy
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_heading_hierarchy.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_heading_hierarchy.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary heading hierarchy
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-heading-hierarchy
          path: artifacts/plan_2_8_digest/summary_heading_hierarchy.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger recent red ratio
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_red_ratio.py \
            --ledger "${ledger}" \
            --last-n 10 \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_red_ratio.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger recent red ratio
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-recent-red-ratio
          path: artifacts/plan_2_8_digest/ledger_red_ratio.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest file age stats
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_file_age_stats.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_file_age_stats.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest file age stats
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-file-age-stats
          path: artifacts/plan_2_8_digest/digest_file_age_stats.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary blockquote count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_blockquote_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_blockquote_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary blockquote count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-blockquote-count
          path: artifacts/plan_2_8_digest/summary_blockquote_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger recent unknown ratio
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_unknown_ratio.py \
            --ledger "${ledger}" \
            --last-n 10 \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_unknown_ratio.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger recent unknown ratio
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-recent-unknown-ratio
          path: artifacts/plan_2_8_digest/ledger_unknown_ratio.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest empty files
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_empty_files.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_empty_files.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest empty files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-empty-files
          path: artifacts/plan_2_8_digest/digest_empty_files.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary horizontal-rule count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_hr_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_hr_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary horizontal-rule count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-hr-count
          path: artifacts/plan_2_8_digest/summary_hr_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger median gap
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_median_gap.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_median_gap.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger median gap
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-median-gap
          path: artifacts/plan_2_8_digest/ledger_median_gap.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest empty ratio
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_empty_ratio.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_empty_ratio.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest empty ratio
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-empty-ratio
          path: artifacts/plan_2_8_digest/digest_empty_ratio.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary inline-code count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_inline_code_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_inline_code_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary inline-code count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-inline-code-count
          path: artifacts/plan_2_8_digest/summary_inline_code_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger unique statuses
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_unique_statuses.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_unique_statuses.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger unique statuses
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-unique-statuses
          path: artifacts/plan_2_8_digest/ledger_unique_statuses.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest size sum
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_size_sum.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_size_sum.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest size sum
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-size-sum
          path: artifacts/plan_2_8_digest/digest_size_sum.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary table count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_table_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_table_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary table count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-table-count
          path: artifacts/plan_2_8_digest/summary_table_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger first green age
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_first_green_age.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_first_green_age.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger first green age
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-first-green-age
          path: artifacts/plan_2_8_digest/ledger_first_green_age.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest largest file
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_largest_file.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_largest_file.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest largest file
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-largest-file
          path: artifacts/plan_2_8_digest/digest_largest_file.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary link count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_link_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_link_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary link count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-link-count
          path: artifacts/plan_2_8_digest/summary_link_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger latest captured_at
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_latest_captured_at.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_latest_captured_at.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger latest captured_at
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-latest-captured-at
          path: artifacts/plan_2_8_digest/ledger_latest_captured_at.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest smallest file
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_smallest_file.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_smallest_file.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest smallest file
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-smallest-file
          path: artifacts/plan_2_8_digest/digest_smallest_file.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary list count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_list_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_list_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary list count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-list-count
          path: artifacts/plan_2_8_digest/summary_list_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger oldest captured_at
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_oldest_captured_at.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_oldest_captured_at.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger oldest captured_at
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-oldest-captured-at
          path: artifacts/plan_2_8_digest/ledger_oldest_captured_at.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest top extension
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_ext_top.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_ext_top.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest top extension
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-ext-top
          path: artifacts/plan_2_8_digest/digest_ext_top.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary ordered list count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_ordered_list_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_ordered_list_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary ordered list count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-ordered-list-count
          path: artifacts/plan_2_8_digest/summary_ordered_list_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger captures per day
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_captures_per_day.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_captures_per_day.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger captures per day
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-captures-per-day
          path: artifacts/plan_2_8_digest/ledger_captures_per_day.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest tiny files
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_tiny_files.py \
            --artifact-dir "${dir}" \
            --threshold-bytes 100 \
            --format md \
            --output artifacts/plan_2_8_digest/digest_tiny_files.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest tiny files
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-tiny-files
          path: artifacts/plan_2_8_digest/digest_tiny_files.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary sha256
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_sha256.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_sha256.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary sha256
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-sha256
          path: artifacts/plan_2_8_digest/summary_sha256.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger longest gap
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_longest_gap.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_longest_gap.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger longest gap
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-longest-gap
          path: artifacts/plan_2_8_digest/ledger_longest_gap.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest duplicate sizes
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_duplicate_sizes.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_duplicate_sizes.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest duplicate sizes
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-duplicate-sizes
          path: artifacts/plan_2_8_digest/digest_duplicate_sizes.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary longest line
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_longest_line.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_longest_line.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary longest line
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-longest-line
          path: artifacts/plan_2_8_digest/summary_longest_line.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger green streak history
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_green_streak_history.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_green_streak_history.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger green streak history
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-green-streak-history
          path: artifacts/plan_2_8_digest/ledger_green_streak_history.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest oldest file
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_oldest_file.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_oldest_file.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest oldest file
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-oldest-file
          path: artifacts/plan_2_8_digest/digest_oldest_file.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary footnote count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_footnote_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_footnote_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary footnote count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-footnote-count
          path: artifacts/plan_2_8_digest/summary_footnote_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger first red
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_first_red.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_first_red.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger first red
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-first-red
          path: artifacts/plan_2_8_digest/ledger_first_red.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest newest file
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_newest_file.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_newest_file.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest newest file
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-newest-file
          path: artifacts/plan_2_8_digest/digest_newest_file.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary bold count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_bold_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_bold_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary bold count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-bold-count
          path: artifacts/plan_2_8_digest/summary_bold_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger first amber
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_first_amber.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_first_amber.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger first amber
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-first-amber
          path: artifacts/plan_2_8_digest/ledger_first_amber.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest median mtime
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_median_mtime.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_median_mtime.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest median mtime
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-median-mtime
          path: artifacts/plan_2_8_digest/digest_median_mtime.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary strikethrough count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_strikethrough_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_strikethrough_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary strikethrough count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-strikethrough-count
          path: artifacts/plan_2_8_digest/summary_strikethrough_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger first unknown
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_first_unknown.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_first_unknown.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger first unknown
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-first-unknown
          path: artifacts/plan_2_8_digest/ledger_first_unknown.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest mtime span
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_mtime_span.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_mtime_span.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest mtime span
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-mtime-span
          path: artifacts/plan_2_8_digest/digest_mtime_span.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary reference defs
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_reference_defs.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_reference_defs.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary reference defs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-reference-defs
          path: artifacts/plan_2_8_digest/summary_reference_defs.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger last red
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_last_red.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_last_red.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger last red
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-last-red
          path: artifacts/plan_2_8_digest/ledger_last_red.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest per-extension bytes
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_per_ext_bytes.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_per_ext_bytes.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest per-extension bytes
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-per-ext-bytes
          path: artifacts/plan_2_8_digest/digest_per_ext_bytes.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary HTML tag count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_html_tag_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_html_tag_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary HTML tag count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-html-tag-count
          path: artifacts/plan_2_8_digest/summary_html_tag_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 ledger last amber
        if: always()
        shell: bash
        run: |
          set +e
          ledger="artifacts/plan_2_8_digest/state_ledger.jsonl"
          [ -s "${ledger}" ] || exit 0
          python scripts/plan_2_8_ledger_last_amber.py \
            --ledger "${ledger}" \
            --format md \
            --output artifacts/plan_2_8_digest/ledger_last_amber.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 ledger last amber
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-ledger-last-amber
          path: artifacts/plan_2_8_digest/ledger_last_amber.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 digest word count
        if: always()
        shell: bash
        run: |
          set +e
          dir="artifacts/plan_2_8_digest"
          [ -d "${dir}" ] || exit 0
          python scripts/plan_2_8_digest_word_count.py \
            --artifact-dir "${dir}" \
            --format md \
            --output artifacts/plan_2_8_digest/digest_word_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 digest word count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-digest-word-count
          path: artifacts/plan_2_8_digest/digest_word_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Plan 2.8 weekly summary autolink count
        if: always()
        shell: bash
        run: |
          set +e
          summary="artifacts/plan_2_8_digest/weekly_summary.md"
          [ -s "${summary}" ] || exit 0
          python scripts/plan_2_8_weekly_summary_autolink_count.py \
            --summary "${summary}" \
            --format md \
            --output artifacts/plan_2_8_digest/summary_autolink_count.md \
            > /dev/null
          true

      - name: Upload Plan 2.8 weekly summary autolink count
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: plan-2-8-weekly-summary-autolink-count
          path: artifacts/plan_2_8_digest/summary_autolink_count.md
          if-no-files-found: ignore
          retention-days: 365

      - name: Open drift-alert issue
        if: steps.resolve_alerts.outputs.has_alerts == 'True'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          set -euo pipefail
          # Re-render the issue body with the workflow-run URL footer
          # so the link survives even after the artifact is gone.
          history="$(find artifacts/latest_bench -name plan_2_8_history.jsonl -print -quit)"
          python scripts/plan_2_8_trend_digest.py \
            --history            "${history}" \
            --lookback-days      "${{ inputs.lookback_days || '7' }}" \
            --alert-threshold-pp "${{ inputs.alert_threshold_pp || '0.05' }}" \
            --min-events         "${{ inputs.min_events || '30' }}" \
            --format             issue \
            --run-url            "${RUN_URL}" \
            --output             artifacts/plan_2_8_digest/issue_body.md \
            > /dev/null
          title="Plan 2.8 weekly digest: drift alerts ($(date -u +%Y-%m-%d))"
          # De-dup: if an open drift-alert issue exists, append a
          # comment instead of opening a fresh one. This keeps the
          # backlog readable across consecutive alerting weeks.
          existing="$(gh issue list \
            --label plan-2.8 --label drift-alert \
            --state open --json number --jq '.[0].number' || echo '')"
          if [ -n "${existing}" ]; then
            gh issue comment "${existing}" \
              --body-file artifacts/plan_2_8_digest/issue_body.md
          else
            gh issue create \
              --title "${title}" \
              --body-file artifacts/plan_2_8_digest/issue_body.md \
              --label plan-2.8,drift-alert
          fi

      - name: Close drift-alert issues when alerts cleared
        if: steps.resolve_alerts.outputs.has_alerts == 'False'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        shell: bash
        run: |
          set -euo pipefail
          # When the weekly digest finds no comparable slice over
          # threshold, close any still-open drift-alert issues with
          # a short note pointing at this run. Idempotent: a no-op
          # when no matching open issue exists.
          numbers="$(gh issue list \
            --label plan-2.8 --label drift-alert \
            --state open --json number --jq '.[].number' || echo '')"
          if [ -z "${numbers}" ]; then
            echo "No open drift-alert issues to close."
            exit 0
          fi
          for n in ${numbers}; do
            gh issue comment "${n}" \
              --body "Plan 2.8 weekly digest now reports no slices over threshold. Closing automatically. Run: ${RUN_URL}"
            gh issue close "${n}" --reason completed
          done

      - name: Plan 2.8 history snapshot diff (last two)
        if: always()
        shell: bash
        run: |
          set +e
          history="$(find artifacts/latest_bench -name plan_2_8_history.jsonl -print -quit)"
          if [ -z "${history}" ]; then
            echo "No history found; skipping diff."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_history_diff.py \
            --history "${history}" \
            --output  artifacts/plan_2_8_digest/snapshot_diff.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/snapshot_diff.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/snapshot_diff.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Plan 2.8 top movers (30-day window)
        if: always()
        shell: bash
        run: |
          set +e
          history="$(find artifacts/latest_bench -name plan_2_8_history.jsonl -print -quit)"
          if [ -z "${history}" ]; then
            echo "No history found; skipping top-movers."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_top_movers.py \
            --history      "${history}" \
            --lookback-days 30 \
            --min-events    "${{ inputs.min_events || '30' }}" \
            --top-n         5 \
            --output        artifacts/plan_2_8_digest/top_movers.md \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/top_movers.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/top_movers.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Plan 2.8 slice coverage
        if: always()
        shell: bash
        run: |
          set +e
          history="$(find artifacts/latest_bench -name plan_2_8_history.jsonl -print -quit)"
          if [ -z "${history}" ]; then
            echo "No history found; skipping coverage."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_coverage.py \
            --history    "${history}" \
            --min-events "${{ inputs.min_events || '30' }}" \
            --output     artifacts/plan_2_8_digest/coverage.md \
            > /dev/null
          python scripts/plan_2_8_coverage.py \
            --history    "${history}" \
            --min-events "${{ inputs.min_events || '30' }}" \
            --format     json \
            --output     artifacts/plan_2_8_digest/coverage.json \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/coverage.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/coverage.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true

      - name: Plan 2.8 slice stability (last 8 snapshots)
        if: always()
        shell: bash
        run: |
          set +e
          history="$(find artifacts/latest_bench -name plan_2_8_history.jsonl -print -quit)"
          if [ -z "${history}" ]; then
            echo "No history found; skipping stability."
            exit 0
          fi
          mkdir -p artifacts/plan_2_8_digest
          python scripts/plan_2_8_history_stability.py \
            --history    "${history}" \
            --window     8 \
            --min-events "${{ inputs.min_events || '30' }}" \
            --output     artifacts/plan_2_8_digest/stability.md \
            > /dev/null
          python scripts/plan_2_8_history_stability.py \
            --history    "${history}" \
            --window     8 \
            --min-events "${{ inputs.min_events || '30' }}" \
            --format     json \
            --output     artifacts/plan_2_8_digest/stability.json \
            > /dev/null
          if [ -s artifacts/plan_2_8_digest/stability.md ] \
              && [ -n "${GITHUB_STEP_SUMMARY:-}" ]; then
            cat artifacts/plan_2_8_digest/stability.md \
              >> "${GITHUB_STEP_SUMMARY}"
          fi
          true
