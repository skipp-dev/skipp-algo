# Optional ML dependencies for the C10 ML-Layer.
# The ml/ pipeline runs end-to-end on the always-on numpy LogisticBaseline
# without these. Install only when training XGBoost/LightGBM models.
xgboost==3.2.0
lightgbm==4.6.0
scikit-learn==1.9.0
optuna==4.9.0
shap==0.52.0
joblib==1.5.3
