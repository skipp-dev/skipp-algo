{
  "schema_version": "v2",
  "purpose": "Pin the HERO vocabulary frozensets that future ML-layer trainers (C10) will consume as categorical feature inputs.",
  "source_module": "scripts/smc_hero_state.py",
  "source_symbols": [
    "HERO_TRUST_VOCAB",
    "HERO_SETUP_QUALITY_VOCAB",
    "HERO_ACTION_VOCAB",
    "HERO_BIAS_VOCAB",
    "HERO_MARKET_MODE_VOCAB",
    "HERO_RISK_VOCAB"
  ],
  "pinned_source_sha256": "80b89eecb99dd68048eb7d693bc0ec8d1f27587cb8bac8a6480f285c3b8df165",
  "sha_method": {
    "tool": "ast.unparse",
    "scope": "module-level AnnAssign or Assign nodes whose target name starts with HERO_ and ends with _VOCAB",
    "joiner": "\\n\\n",
    "encoding": "utf-8",
    "note": "Both typed (AnnAssign) and untyped (Assign) bindings are hashed so a drive-by removal of the type annotation cannot silently bypass the pin."
  },
  "drift_policy": {
    "cosmetic_docstring": "Update SHA only; no schema_version bump. Add a one-line CHANGELOG note.",
    "new_vocab_value_added": "MAJOR — bump schema_version to v2 and add ADR (training-data implication).",
    "vocab_value_removed_or_renamed": "MAJOR — bump schema_version to v2 and add migration ADR.",
    "new_vocab_set_added": "MINOR — append to source_symbols and update SHA; no schema_version bump."
  },
  "vocabularies": {
    "HERO_TRUST_VOCAB": ["healthy", "warmup", "degraded", "stale", "unavailable"],
    "HERO_SETUP_QUALITY_VOCAB": ["high", "good", "ok", "low", "unavailable"],
    "HERO_ACTION_VOCAB": ["ACTIVE", "WATCH", "AVOID", "BLOCKED"],
    "HERO_BIAS_VOCAB": ["LONG", "SHORT", "FLAT", "UNKNOWN"],
    "HERO_MARKET_MODE_VOCAB": ["BULLISH", "BEARISH", "NEUTRAL", "RISK_OFF", "UNKNOWN"],
    "HERO_RISK_VOCAB": ["", "DATA_STALE", "EVENT_RISK", "VOLATILITY", "PROVIDER_GAPS"],
    "HERO_RISK_VOCAB_note": "Empty string is a Pine-gated sentinel meaning 'no dominant risk' (gated by ``!= \"\"`` on the consumer side)."
  },
  "consumer_contract": {
    "categorical_encoding": "These vocabularies are the authoritative categorical-feature set for the future ML layer. Encoders must use the exact string values above.",
    "drift_changes_break": "Any vocabulary drift bumps schema_version to v2 and requires a CHANGELOG + library_field_version bump per ADR-0006."
  },
  "audit_trail": {
    "created_at": "2026-04-26",
    "created_by_pr": "sprint/c10-foundation",
    "companion_test": "tests/test_hero_observed_vocab_pin.py (already enforces vocab-drift detection at the source)"
  }
}
